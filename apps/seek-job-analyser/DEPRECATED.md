# DEPRECATED — replaced by apps/seek-ghost-buster/

This Node.js scaffold has been abandoned. The SEEK job analyser is now a Chrome/Edge browser extension at:

```
apps/seek-ghost-buster/
```

**To clean up:**

```bash
cd ~/cybersurf/apps
rm -rf seek-job-analyser
```

(or delete from your file explorer — either is fine)

— The Coder
